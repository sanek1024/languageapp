name: language_learning_app
description: A language learning app built with Flutter.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite:
  path_provider:

  # Add other dependencies here as needed

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  cloud_firestore: 

flutter:
  uses-material-design: true

  assets:
    - assets/images/
